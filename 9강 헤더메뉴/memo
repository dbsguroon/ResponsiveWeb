reset 작업해준것

1. 여백초기화
2. a태그 초기화
3. 폰트초기화

참고로 파비콘 이미지 넣는것에서 ico라는 파일이 있는데 화질이 구림
그래서 PNG 로 넣어주었지만 익스플로러에서 안나올수도 있는 단점이 있음

인라인구조에서는 padding이 좌우만 먹힘 (위,아래 안먹음)
block 구조로 바꾸어주면 되는데  block 구조로 바꾸면 나란히 정렬히 못해서
display:inline-block으로 바꾸어줌

transition 문법

transition : transition-property | transition-duration | transition-timing-function | transition-delay
 
transition-property : css 속성 사용
transition-duration : 실행시간 , 속성을 항상 지정해주어야함(기본값이 0s이라서)
transition-timing-function :트랜지션이 실행되는 동안 속도 설정
transition-delay : 언제 트렌지션을 시작할지 지정


반응형할때는 가로스크롤이 생기면안됨 (% 값으로 지정)

.container {width:1200px;}

@media only all and (max-width:1220px){
   .container{
      width:100%;
   }
}